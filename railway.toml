[build]
builder = "RAILPACK"
buildCommand = "pip install -r backend/requirements.txt"

[deploy]
startCommand = "pip install -r backend/requirements.txt && python -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
